<h1>Fridge show</h1>

<%= @fridge.name %>

<p>items:</p>
<% @fridge.items.each do |item| %>
    <% item.options.each do |o|%>
        <%= o.name %>
    <% end %>
<% end %>


<p>
    <%= link_to 'Add item', new_fridge_item_path(@fridge) %>
</p>

<p>
    <%= link_to 'All Fridges', fridges_path %>
</p>

